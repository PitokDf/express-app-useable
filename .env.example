# Server Configuration
PORT=6789
NODE_ENV=development
SERVICE_NAME=express-framework
BASE_URL=http://localhost:6789
CLIENT_URL=http://localhost:3000

# CORS Configuration (comma-separated list of allowed origins)
# In production, specify exact domains: https://yourapp.com,https://admin.yourapp.com
# In development, you can use: http://localhost:3000,http://localhost:3001
ALLOWED_ORIGINS=http://localhost:3000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here

# Database Configuration (Prisma will use DATABASE_URL)
DATABASE_URL="postgresql://postgres:password@localhost:5432/mydb?schema=public"

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=90000
RATE_LIMIT_MAX_REQUESTS=100

# Email Configuration
EMAIL_PROVIDER=smtp
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-email-password
EMAIL_FROM=noreply@yourapp.com

# SMTP Configuration (if using SMTP provider)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Redis Configuration (for background jobs and caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# File Upload Configuration
UPLOAD_MAX_SIZE=10485760
UPLOAD_DIR=uploads

# API Versioning
API_DEFAULT_VERSION=1.0
API_VERSION_HEADER=API-Version

# Cache Configuration
CACHE_TTL=3600
CACHE_CHECK_PERIOD=600

# Health Check Configuration
HEALTH_CHECK_TIMEOUT=5000

# Development/Production specific settings
# In production, make sure to set:
# - Strong JWT_SECRET
# - Proper DATABASE_URL
# - Email credentials
# - Redis connection if using background jobs
# - CLIENT_URL to your frontend domain